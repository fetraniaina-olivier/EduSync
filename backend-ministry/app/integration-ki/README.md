# Contrat d'intégration Ministère ↔ École
- API Base URL : http://localhost:8000
- Endpoint Push : POST /api/v1/sync/push
- Endpoint Pull : POST /api/v1/sync/pull
- Format payload : Voir `ministry-api-contract.json`
- Auth : Bearer Token (à implémenter à l'étape 2)